#name: Build Swift DEB

#on:
#  push:            # 🔁 Run on every push to any branch
#  workflow_dispatch:

#jobs:
#  build-deb:
#    runs-on: ubuntu-latest

#    env:
#      NAME: test           # Replace if needed
#      VERSION: 0.0.1               # You can extract from Git in a future step

#    steps:
#      - name: Checkout source
#        uses: actions/checkout@v4

#      - name: Install Swift and Debian packaging tools
#        run: |
#          sudo apt update
#          sudo apt install -y curl git dpkg-dev clang libcurl4-openssl-dev libssl-dev libatomic1
#          curl -O https://download.swift.org/swift-5.9-release/ubuntu2204/swift-5.9-RELEASE/swift-5.9-RELEASE-ubuntu22.04.tar.gz
#          tar -xzf swift-5.9-RELEASE-ubuntu22.04.tar.gz
#          echo "$(pwd)/swift-5.9-RELEASE-ubuntu22.04/usr/bin" >> $GITHUB_PATH

#      - name: Verify Swift is installed
#        run: swift --version

#      - name: Build .deb package
#        run: make deb NAME=$NAME VERSION=$VERSION

#      - name: Upload .deb as artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: ${{ env.NAME }}-deb
#          path: build-deb/**/*.deb